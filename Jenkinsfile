pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'dev branch'
            }
        }
        stage('Deploy') {
            steps {
                echo 'dev branch'
            }
        }
    }
}
