pipeline {
    agent any
    stages {
        stage('Build') {n
            steps {
                sh 'hostname'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}
